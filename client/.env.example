# フロントエンド環境変数テンプレート
#
# AWS等へデプロイする際、APIサーバー（バックエンド）のURLが異なる場合は設定してください。
# 例: VITE_API_BASE_URL=https://xxx.execute-api.ap-northeast-1.amazonaws.com/api
#
# 空欄（またはコメントアウト）の場合、フロントエンドと同じドメインの `/api` に向けて通信します。
# （CloudFrontで `/api/*` や `/uploads/*` をAPI Gatewayにルーティングする構成を推奨します）

# VITE_API_BASE_URL=

VITE_PORT=5173
VITE_HTTPS=true
