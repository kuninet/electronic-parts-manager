# AWS デプロイ後のフロントエンド環境変数 (例)
# 実際の値は deploy-frontend.sh が自動生成します。
# 手動設定する場合は、このファイルをコピーして .env.production を作成してください。
#
# CloudFront のドメインを指定します（Lambda URL ではなく CloudFront URL を使うこと）
VITE_API_BASE_URL=https://<あなたのCloudFrontドメイン>.cloudfront.net/api
